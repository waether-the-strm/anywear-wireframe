import React from "react";
import ReactDOM from "react-dom/client";
import Wireframe from "../anywear_wireframe";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Wireframe />
  </React.StrictMode>
);
